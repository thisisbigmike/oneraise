import ComingSoon from '@/components/ui/ComingSoon';

export const metadata = { title: 'Cards — OneRaise' };

export default function CardsPage() {
  return (
    <ComingSoon
      eyebrow="Products · Cards"
      title="A virtual card for your balance"
      description="Spend what you raise anywhere — a virtual USD card linked to your OneRaise balance. In the works."
    />
  );
}
